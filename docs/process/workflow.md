# Workflow (STRICT) — Kurskarten Monorepo

Authority:
SPEC > PROCESS > AGENT ROLE > TASK

Operating mode:
- Work in small vertical slices.
- No refactors/renames/restructure unless requested.
- Default to MVP interpretations when ambiguous; list assumptions explicitly.
- After each step: state what changed, how to verify, and whether stop conditions are met.

Definition of "done" for a step:
- code compiles
- basic run instructions given
- at least one manual verification action described
