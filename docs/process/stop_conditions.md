# Stop conditions

Stop immediately when ALL are true:
- MVP endpoints exist and respond with example data
- Web app renders a graph for a selected year
- Clicking a node opens a detail panel
- Performance is acceptable for demo dataset (no obvious stutter on slider)
